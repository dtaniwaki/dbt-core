# Custom Build

Base: v1.11.7
Date: 2026-04-02

## Included PRs

- https://github.com/dbt-labs/dbt-core/pull/12604 — feat: add config.tags and +tags support for metrics
- https://github.com/dbt-labs/dbt-core/pull/12628 — feat: add --no-full-refresh flag to dbt compile

## Conflict Resolutions

- `.github/workflows/cut-release-branch.yml`: kept v1.11.7 version (unrelated to PR changes)
- `CHANGELOG.md`: kept v1.11.7 version (unrelated to PR changes)
- `CONTRIBUTING.md`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/__version__.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/compilation.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/config/runtime.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/contracts/project.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/deprecations.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/jsonschemas/jsonschemas.py`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/jsonschemas/project/0.0.110.json`: kept v1.11.7 version (unrelated to PR changes)
- `core/dbt/jsonschemas/resources/latest.json`: kept v1.11.7 version (unrelated to PR changes)
- `core/pyproject.toml`: kept v1.11.7 version (unrelated to PR changes)
- `tests/functional/configs/test_warn_error_options.py`: kept v1.11.7 version (unrelated to PR changes)
- `tests/functional/constraints/test_foreign_key_constraints.py`: kept v1.11.7 version (unrelated to PR changes)
- `tests/functional/deprecations/test_deprecations.py`: kept v1.11.7 version (unrelated to PR changes)
- `tests/functional/init/test_init.py`: kept v1.11.7 version (unrelated to PR changes)
- `tests/unit/parser/test_parser.py`: kept v1.11.7 version (unrelated to PR changes)
- `tests/unit/test_events.py`: kept v1.11.7 version (unrelated to PR changes)
